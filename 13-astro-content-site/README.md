# 13 — Astro content site (non-Docker)

Starter static-first untuk editorial agency, SEO blog, atau knowledge base. Tidak ada JavaScript client secara default.

```bash
npm install
npm run dev
npm run build
```

Deploy folder `dist/` ke static hosting. Tambah MDX/content collections bila konten mulai dikelola banyak penulis.
