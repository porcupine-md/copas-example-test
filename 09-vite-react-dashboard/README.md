# 09 — Vite + React agency dashboard (non-Docker)

Starter dashboard untuk client portal, internal agency OS, atau admin ringan.

```bash
npm install
npm run dev
npm run build
npm run start
```

Deploy sebagai static site: artefak build ada di `dist/`. `npm run start` hanya preview lokal, bukan server produksi.
