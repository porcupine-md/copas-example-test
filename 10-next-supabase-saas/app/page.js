import styles from './page.module.css';
export default function Home() { const configured = Boolean(process.env.NEXT_PUBLIC_SUPABASE_URL); return <main className={styles.main}><p className={styles.eyebrow}>NEXT.JS × SUPABASE</p><h1>Ship the useful<br/>version first.</h1><p className={styles.copy}>A clean SaaS foundation: App Router, a Supabase client dependency, environment boundaries, and a health endpoint.</p><div className={styles.card}><span>Supabase configuration</span><b className={configured ? styles.ok : styles.wait}>{configured ? 'Ready to connect' : 'Add environment values'}</b><code>NEXT_PUBLIC_SUPABASE_URL</code></div><a href="/api/health">Check deployment health →</a></main>; }
