# 02 — Next.js + Turbopack

A single-page React app, built with Next.js and Turbopack.

```bash
npm install
npm run dev
npm run build
npm run start
```

Deploy this directory as a Node/Next.js project. The server honors `PORT` when started with `npm run start`.
